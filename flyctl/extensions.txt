Extensions are additional functionality that can be added to your Fly apps

Usage:
  flyctl extensions [command]

Aliases:
  extensions, extensions, ext

Available Commands:
  sentry      Setup a Sentry project for this app

Flags:
  -h, --help   help for extensions

Global Flags:
  -t, --access-token string   Fly API Access Token
      --verbose               verbose output

Use "flyctl extensions [command] --help" for more information about a command.
